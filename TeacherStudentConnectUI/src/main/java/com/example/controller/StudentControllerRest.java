package com.example.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class StudentControllerRest {
	
	/*
	 * @Autowired private StudentRepository studRepo;
	 */
	
	
	/*
	 * @PostMapping("/submitstudent") public Student saveStudent(@RequestBody
	 * Student student) { return studRepo.saveStudent(student); }
	 */


}
